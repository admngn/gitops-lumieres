apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: todos-api-dev

resources:
  - namespace.yaml
  - ../../base
  - ../../base/game-2048

patches:
  - path: todos-api/todos-api-dev.yaml
    target:
      kind: Deployment
      name: todo-api
